
import Test from '../src/index'
import { describe, describeClass, its, run, it, itAdd, itClass } from 'axiba-unit-test';


let test = new Test();

describeClass('测试', test, () => {
     itClass('init', () => {
        itAdd(['axiba 万岁！!!'], value => value == 1);
    });
})

run();